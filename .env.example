# Database (PostgreSQL)
# Local:   postgresql://postgres:postgres@localhost:5432/codestream
# Vercel:  postgres://... (from Vercel Postgres dashboard)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/codestream"

# JWT secret for admin sessions
JWT_SECRET="change-me-in-production"
