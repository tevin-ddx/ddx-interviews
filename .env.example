# Database (PostgreSQL)
# Local:   postgresql://postgres:postgres@localhost:5432/ddx
# Vercel:  postgres://... (from Vercel Postgres dashboard)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/ddx"

# JWT secret for admin sessions
JWT_SECRET="change-me-in-production"
